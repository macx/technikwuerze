title: Phase

columns:
  - width: 1/2
    sections:
      episodes:
        type: pages
        label: Folgen
        info: '{{ page.podcasterSubtitle }}<br />{{ page.date.toDate("d.m.Y") }}'
        templates:
          - episode
        search: true
        sortBy: date desc
        layout: list

  - width: 1/2
    sections:
      lead-blocks:
        extends: fields/lead-blocks
