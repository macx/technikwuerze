{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "🚀 Development",
      "type": "node-terminal",
      "request": "launch",
      "preLaunchTask": "Prepare Development Environment",
      "command": ". ./.env 2>/dev/null || true; [ -n \"$DEV_HOST\" ] || DEV_HOST=localhost; [ -n \"$DEV_PHP_PORT\" ] || DEV_PHP_PORT=8000; [ -n \"$DEV_VITE_PORT\" ] || DEV_VITE_PORT=5173; (while ! nc -z \"$DEV_HOST\" \"$DEV_VITE_PORT\" || ! nc -z \"$DEV_HOST\" \"$DEV_PHP_PORT\"; do sleep 1; done; open -na \"Google Chrome\" --args --incognito --new-window \"http://$DEV_HOST:$DEV_PHP_PORT/\" \"http://$DEV_HOST:$DEV_PHP_PORT/panel\") >/dev/null 2>&1 & pnpm run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
